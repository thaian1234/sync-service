# .env.docker - CMS Service Configuration for Docker Compose
# This file is used when running the service inside Docker containers

# Database Configuration (using Docker service name)
DATABASE_HOST=mysql-cms
DATABASE_PORT=3306
DATABASE_USER=user
DATABASE_PASSWORD=password
DATABASE_NAME=cms_db

# Kafka Configuration (using Docker service name and internal port)
KAFKA_BROKER=kafka:9092
KAFKA_CLIENT_ID=cms-service
KAFKA_CONSUMER_GROUP=cms-service-group

# Application
PORT=3002
NODE_ENV=development

# Redis Configuration (using Docker service name)
REDIS_HOST=redis
REDIS_PORT=6379
# REDIS_PASSWORD=your_redis_password  # Uncomment if Redis requires authentication
REDIS_DB=0
# TTL in seconds - how long to keep processed event IDs in cache
REDIS_TTL=300  # 5 minutes default
